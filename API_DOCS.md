# Boss AI 시스템 API 문서

## 시스템 개요

이 문서는 Boss AI 시스템의 구성 요소인 `EFlyingState`, `ETagCategory`, 그리고 `EEffectFunctionType` 에 대한 API 레퍼런스를 제공합니다. 이 시스템은 Boss 캐릭터의 비행 상태, 태그 기반 동작 분류, 그리고 이펙트 함수 유형을 관리하는 데 사용됩니다. 각 클래스는 Boss AI의 다양한 측면을 제어하며, 함께 작동하여 복잡하고 다양한 Boss 행동을 구현합니다.  이 시스템을 통해 개발자는 Boss의 상태, 공격 패턴, 이펙트 등을 유연하게 정의하고 제어할 수 있습니다.

## 클래스 카테고리별 분석

### 1. 컴포넌트 클래스

#### EFlyingState (Component)

* **핵심 기능:** Boss 캐릭터의 비행 상태를 나타내는 열거형 클래스입니다. 이 컴포넌트는 Boss 캐릭터의 현재 비행 상태를 추적하고, 상태 변화에 따른 애니메이션 및 로직을 처리합니다.

* **멤버:**
    * `FLYING`: 비행 중.
    * `HOVERING`: 공중에서 정지.
    * `LANDED`: 착륙.
    * `FALLING`: 낙하 중.

* **사용 가이드:** `EFlyingState` 는 Boss 캐릭터의 움직임 컴포넌트에서 사용되어야 합니다. 현재 비행 상태를 설정하고, GetFlyingState() 함수를 통해 현재 상태를 가져올 수 있습니다.  상태 변경 시, 관련 애니메이션 및 로직을 업데이트해야 합니다.

* **베스트 프랙티스:**  상태 변화 로직을 명확하게 분리하여 가독성과 유지보수성을 향상시키세요.  각 상태에 대한 애니메이션 및 이펙트를 적절히 연결하여 시각적인 피드백을 제공하세요.

### 2. 데이터 클래스

#### ETagCategory (Data)

* **핵심 기능:** Boss의 행동 및 특징을 분류하는 태그 카테고리를 정의하는 열거형 클래스입니다. 다양한 태그를 사용하여 Boss의 공격 패턴, 약점, 특수 능력 등을 구분할 수 있습니다.

* **멤버:**
    * `ATTACK`: 공격 관련 태그.
    * `DEFENSE`: 방어 관련 태그.
    * `MOVEMENT`: 이동 관련 태그.
    * `SPECIAL`: 특수 능력 관련 태그.

* **사용 가이드:**  `ETagCategory`는 Boss AI 로직에서 특정 행동을 트리거하거나 조건을 판별하는 데 사용됩니다. 각 태그는 문자열 또는 다른 데이터와 연결되어 더욱 세분화된 분류를 가능하게 합니다.

* **베스트 프랙티스:** 태그 시스템을 잘 설계하여 Boss AI의 확장성과 유연성을 확보하세요.  태그의 의미를 명확하게 정의하고 일관성 있게 사용하는 것이 중요합니다.

### 3. 애니메이션 노티파이 클래스

#### EEffectFunctionType (Notifies)

* **핵심 기능:** 애니메이션 노티파이에서 호출할 이펙트 함수의 유형을 정의하는 열거형 클래스입니다. 이를 통해 애니메이션 이벤트에 따라 특정 이펙트를 발생시킬 수 있습니다.

* **멤버:**
    * `SPAWN_EFFECT`: 이펙트 생성.
    * `ACTIVATE_EFFECT`: 이펙트 활성화.
    * `DEACTIVATE_EFFECT`: 이펙트 비활성화.
    * `PLAY_SOUND`: 사운드 재생.

* **사용 가이드:** `EEffectFunctionType` 는 애니메이션 블루프린트의 Notify에서 사용되어 특정 프레임에 이펙트를 실행합니다.  각 함수 유형에 따라 적절한 파라미터를 전달하여 이펙트를 제어할 수 있습니다.

* **베스트 프랙티스:**  애니메이션과 이펙트의 동기화를 위해 정확한 타이밍에 노티파이를 설정하세요.  다양한 이펙트 함수 유형을 활용하여 시각적 연출을 풍부하게 만들 수 있습니다.


## 사용 가이드

Boss AI 시스템을 사용하려면, Boss 캐릭터에 `EFlyingState` 컴포넌트를 추가하고, AI 로직에서 `ETagCategory` 를 사용하여 행동을 분류합니다. 애니메이션 블루프린트에서는 `EEffectFunctionType` 노티파이를 사용하여 이펙트를 발생시킵니다.


## 베스트 프랙티스

* 각 클래스의 역할과 기능을 명확하게 이해하고 사용합니다.
* 클래스 간의 관계를 고려하여 효율적인 시스템을 구축합니다.
* 주석을 충분히 작성하여 코드의 가독성과 유지보수성을 향상시킵니다.
* 디버깅 기능을 활용하여 문제 발생 시 신속하게 해결합니다.
* 성능 최적화를 고려하여 불필요한 연산을 최소화합니다.
